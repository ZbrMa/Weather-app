import { MyLocations } from "../blocks/myLocations";
import { Layout } from "./layout";

export function Locations() {  

    return(
        <Layout>
            <MyLocations/>
        </Layout>
    );
}